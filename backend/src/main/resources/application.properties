spring.application.name=backend
spring.config.import=optional:file:.env[.properties]
server.port=${PORT:8081}

# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-old-brook-ay4drz8x-pooler.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&channelBinding=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_mMX8j4GngYlT}
spring.datasource.driver-class-name=${DATABASE_DRIVER:org.postgresql.Driver}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# JWT Configuration
jwt.secret=${JWT_SECRET:this_is_a_test_jwt_secret_key_which_must_be_very_long_to_be_secure_fittrack_app_2026}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Gemini API Configuration
gemini.api.key=${GEMINI_API_KEY:your_gemini_api_key_here}
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-latest:generateContent

# CORS Configuration
frontend.url=${FRONTEND_URL:http://localhost:5173}
